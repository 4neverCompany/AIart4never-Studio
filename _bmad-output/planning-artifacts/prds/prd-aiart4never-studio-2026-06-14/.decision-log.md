# Decision Log — AIart4never Studio PRD

Canonical memory + audit trail for this PRD run. Append-only; every decision/override lands here.

## Run metadata
- **Skill:** bmad-prd · **Intent:** Create · **Mode:** headless (no interactive elicitation)
- **Date:** 2026-06-14 · **Operator:** Maurice
- **Primary input:** `docs/PRODUCT-BRIEF.md` (the project brief — comprehensive; decisions D1–D10,
  reuse map, architecture sketch, MiniMax/Exa research).

## Carried-in decisions (from the brief; treated as settled)
- **D1** Standalone product, NOT a module of `c4n-4neverCompanyOS` (OS is an idea, not a program).
- **D2** Windows desktop `.exe` + signed auto-update (Tauri/NSIS + minisign).
- **D3** LLM = MiniMax only (MiniMax-M3), on the MiniMax **Token Plan subscription** (flat, not PAYG); registry stays pluggable.
- **D4** MCP mandatory as the tool layer.
- **D5** Reuse MashupForge by concept — adapt + improve, no blind copy-paste.
- **D6** Compliant growth only — no automated like/comment/follow on third parties.
- **D7** MCP servers AND skills user-managed in-app (Claude-Code-style Customize surface).
- **D8** Ships a CLI alongside the GUI.
- **D9** No bespoke competitor scraper; replace the broken homegrown web-search with a reliable web-research MCP.
- **D10** Project management = Linear ("AIart4never Studio", 16 issues / 5 milestones).
- **Repo Strategy B** (fresh, hardened repo importing/adapting MashupForge libs) — settled.
- **Distribution = public** (private GitHub release distribution is rate-limited + storage-capped).
- **Product name = AIart4never Studio** (was codename "Master4never Agent").

## Verified during research (adversarially)
- MiniMax **Token Plan covers programmatic API/agent use** via a per-team Subscription Key (`sk-cp-`)
  on the OpenAI-compatible endpoint `api.minimax.io/v1`; **M3 explicitly included**; Ultra ≈ 12.5B
  M3 tokens/mo. Unlike Claude/OpenAI consumer plans. → enables FR-20.
- **Web-research backend = Exa (primary) / Tavily (fallback)**; Firecrawl rejected (blocks IG/TikTok/
  YouTube). Exa free tier = 20,000 req/mo (confirmed live). → FR-15, Open Question 4.
- MiniMax-M3 multi-turn tool discipline is **not independently verified**; mitigated by the Approval
  Gate (FR-10) + flat subscription (cost can't run away). → recorded as the model risk posture.

## PRD authoring decisions
- **Stakes calibration:** solo-built but genuine launch-light product → focused PRD (not 2-page
  hobby, not enterprise bloat). Capabilities only; tech → `addendum.md`.
- **Adapt-In clusters pulled in:** Constraints & Guardrails (Safety/Privacy/Cost), Aesthetic & Tone
  (Canon/voice), Platform (Windows desktop), Cross-Cutting NFRs. Enterprise/regulated/embedded
  clusters dropped (not applicable to a single-operator desktop tool).
- **FR scheme:** FR-1..FR-21 across 7 feature groups; each with testable consequences.
- **Open items** captured in §8 (6 open questions), not silently filled. Inferred values tagged
  `[ASSUMPTION]` and indexed §9.

## Status
- `prd.md` frontmatter `status: draft` — produced headless from the brief; **operator should review
  before architecture/stories**. Reviewer gate (Finalize step 3) NOT yet run (deferred — next pass).

## Change — 2026-06-14 (operator addition)
- **FR-22 added** (Connectors feature §4.6): agentic Connector install — the agent installs/adds an
  MCP from an operator-provided **link or command** ("install this MCP: <url>" / `npx -y <server>`),
  parse → **operator-confirm** → register → health-check. Mirrors a coding agent adding MCP.
- **Security posture:** confirm-before-activate (granting a tool is a trust action); install only
  from operator-provided sources, never from observed/scraped content (injection guard); agent-installed
  Connectors stay visible + revocable in the manager. Added a "Tool trust" guardrail to §Constraints.
- Research wf `wcs0yqq6d` returned: **mechanism** verified against primary docs (`mcpServers` config
  shape; stdio vs remote streamable-http; OAuth 2.1 on 401; `claude mcp add`/`add-json`; cursor://
  & vscode: deep-links decode base64/URL-JSON). **Security** posture sourced from OWASP MCP /
  Invariant Labs / Trail of Bits / MCP spec: connector metadata = untrusted (tool-poisoning);
  layered controls (confirm-exact-command, trust-on-first-use pinning, least-privilege OAuth scope,
  sandbox, never from observed content). FR-22 NFR upgraded from `[ASSUMPTION]` → grounded.
- ⚠️ The workflow's **adversarial verify + synth phases did NOT complete** — hit the session usage
  limit (resets ~15:50 Europe/Berlin). Findings are primary-sourced but not adversarially
  re-verified; optional re-verify after reset.
